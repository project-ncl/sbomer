#
# JBoss, Home of Professional Open Source.
# Copyright 2023 Red Hat, Inc., and individual contributors
# as indicated by the @author tags.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
# http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#

quarkus:
  swagger-ui:
    always-include: true
    path: /api
  datasource:
    db-kind: postgresql
  smallrye-openapi:
    info-title: SBOMer service
  hibernate-orm:
    database:
      generation: update # TODO: This is for now, so that we can work on stage, remove this once we maintain the DB properly
  qpid-jms:
    url: amqps://umb.api.redhat.com:5671

sbomer:
  pnc:
    api-url: "${SBOMER_CACHE_SERVICE_URL:orch.psi.redhat.com}"
  domino:
    default-version: "0.0.84"
    additional-args: "--include-non-managed"
  cyclonedx:
    default-version: "2.7.5"
    additional-args: "--batch-mode --no-transfer-progress --quiet"
  umb:
    trigger-sbom-generation: false
    pnc-builds-topic: "Consumer.pncsbomer.prod.VirtualTopic.eng.pnc.>"

"%dev":
  quarkus:
    # class-loading:
    #  parent-first-artifacts: io.quarkus:quarkus-bootstrap-maven-resolver
    datasource:
      username: username
      password: password
      jdbc:
        url: jdbc:postgresql://localhost:5432/sbomer
    hibernate-orm:
      database:
        generation: drop-and-create
      log:
        sql: true
    smallrye-openapi:
      info-title: SBOMer service (development)

  sbomer:
    generation:
      local:
        path: /tmp/sbomer
    pnc:
      api-url: "${SBOMER_CACHE_SERVICE_URL:localhost:7070/pnc/orch}"
    domino:
      default-version: "0.0.84"
      additional-args: "--include-non-managed"
    cyclonedx:
      default-version: "2.7.5"
      additional-args: "--batch-mode --no-transfer-progress"
    umb:
      trigger-sbom-generation: false
      pnc-builds-topic: "Consumer.pncsbomer.nonprod.VirtualTopic.eng.pnc.>"

"%test":
  quarkus:
    devservices:
      enabled: false
    datasource:
      db-kind: h2
      jdbc:
        url: jdbc:h2:mem:default;DB_CLOSE_DELAY=-1;INIT=RUNSCRIPT FROM 'classpath:init.sql'
    hibernate-orm:
      database:
        generation: drop-and-create
    qpid-jms:
      url: amqp://localhost:5672
      username: 
      password: 

  sbomer:
    pnc:
      api-url: "localhost/pnc/orch"
    domino:
      default-version: "0.0.84"
      additional-args: "--include-non-managed"
    cyclonedx:
      default-version: "2.7.5"
      additional-args: "--batch-mode --no-transfer-progress"
    umb:
      trigger-sbom-generation: true
      pnc-builds-topic: "Consumer.pncsbomer.testing.VirtualTopic.eng.pnc.>"
